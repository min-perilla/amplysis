% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_rarefaction.R
\name{data_rarefy}
\alias{data_rarefy}
\title{Data Rarefaction}
\usage{
data_rarefy(
  file,
  id_col = 0,
  method = "vegan",
  seed = 123,
  replace = F,
  trimOTUs = T,
  tax_table = NULL,
  write_file = TRUE,
  file_name = "table"
)
}
\arguments{
\item{file}{(data.frame) OTU data frame}

\item{id_col}{(integer) The OTU_ID column is in which column, defaulting to 0 means there is no OTU_ID column, and the data is already numeric.}

\item{method}{(character) The data rarefaction can be performed using the "vegan" package or the "phyloseq" package.}

\item{seed}{(integer) (Only applicable when using the "phyloseq" package for data rarefaction) Random seed.}

\item{replace}{(logical) Whether to sample with replacement (TRUE) or without replacement (FALSE).
(1) Sampling with replacement is faster and more memory-efficient;
(2) Sampling with replacement means that the read counts for some OTUs could
be greater than the original count values.}

\item{trimOTUs}{(logical) (Only applicable when using the "phyloseq" package for data rarefaction) Whether to remove OTUs with zero counts for each sample.}

\item{tax_table}{(tax table)}

\item{write_file}{(logical) Whether to save the rarefied file.}

\item{file_name}{(character) Customize the saved file name.}
}
\value{
Return an OTU table that has been rarefied,
or return a list containing a rarefied OTU table and an aligned taxonomy table after rarefaction.
}
\description{
Use the "rrarefy" function from the "vegan" package or the "rarefy_even_depth" function from the "phyloseq" package for data rarefaction.
}
\examples{
\dontrun{data_rarefy(file = otu_table)}
\dontrun{data_rarefy(file = otu_table, id_col = 0, method = "vegan")}
\dontrun{data_rarefy(file = otu_table, id_col = 0, method = "phyloseq", seed = 1231)}
\dontrun{data_rarefy(file = otu, id_col = 1, method = "phyloseq",
trimOTUs = T, tax_table = tax, write_file = T)}

}
